export const publicRoutes: string[] = ["/"];

export const authRoutes: string[] = ["/auth/login", "/auth/register"];

export const adminRoutes = ["/stats"];

export const apiAuthPrefix = "/api/auth";

export const LOGIN_REDIRECT = "/stats";
